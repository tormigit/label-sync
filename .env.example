# GitHub token with access to the source + target repositories.
# Classic PAT: needs 'repo' scope for private repositories.
# Fine-grained PAT: grant access to the specific repos and set "Issues" permission to Read and write.
GITHUB_TOKEN=

# Optional: override default config file path
# LABEL_SYNC_CONFIG=label-sync.yml
